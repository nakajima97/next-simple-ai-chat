import { Layout } from '@/components/Layout';
import { ChatMain } from '../../ui/ChatMain';

const ChatPage = () => {
	return (
		<Layout>
			<ChatMain />
		</Layout>
	);
};

export default ChatPage;
